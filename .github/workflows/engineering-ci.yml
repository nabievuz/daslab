name: Engineering CI

# GitHub only discovers workflows at the repository root, so the engineering
# product's pipeline lives here (not under engineering/.github/) and scopes
# itself to engineering/** changes. pnpm is provisioned via corepack from the
# packageManager field — the same path the DoD documents
# (corepack enable && pnpm install).

on:
  push:
    branches: [main]
    paths:
      - 'engineering/**'
      - '.github/workflows/engineering-ci.yml'
  pull_request:
    paths:
      - 'engineering/**'
      - '.github/workflows/engineering-ci.yml'

permissions:
  contents: read

env:
  COREPACK_ENABLE_DOWNLOAD_PROMPT: '0'

defaults:
  run:
    working-directory: engineering

jobs:
  release-gate:
    name: Engineering 100/100 gate (lint + product-diagnostics)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Set up Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version-file: engineering/.nvmrc

      - name: Provision pnpm via corepack
        run: |
          corepack enable
          corepack prepare pnpm@10.33.0 --activate
          pnpm --version

      - name: Install (frozen lockfile; prepare builds consumed packages)
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm run lint

      - name: Product diagnostics — build + typecheck + test (coverage ≥90%) + audit, must be SCORE = 100/100
        run: node scripts/product-diagnostics.mjs

  integration:
    name: Integration — distributed rate-limit vs real Redis
    runs-on: ubuntu-latest
    services:
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping" --health-interval 5s
          --health-timeout 5s --health-retries 5
    env:
      REDIS_URL: redis://localhost:6379
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Set up Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version-file: engineering/.nvmrc

      - name: Provision pnpm via corepack
        run: |
          corepack enable
          corepack prepare pnpm@10.33.0 --activate

      - name: Install (frozen lockfile)
        run: pnpm install --frozen-lockfile

      - name: Build (cross-package resolution)
        run: pnpm run build

      - name: Integration tests (two replicas, one Redis — global limit holds)
        run: pnpm run test:integration
